#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
  int Repeat_Number, operand1, operand2;
  scanf("%d", &Repeat_Number);
  for (int i = 0; i < Repeat_Number; i++)
  {
  scanf("%d %d", &operand1, &operand2);
  printf("%d\n", operand1 + operand2);
  }

  return 0;
}
