#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
  int i=0, Number;
  scanf("%d", &Number);

  for(i = 0; i < Number; i++) {
  i = (Number * (Number + 1)) / 2;
  }
  printf("%d", i-1);
}
