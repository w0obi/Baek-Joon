#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
  char Count;
  scanf("%hhd", &Count);

  for (int i = 0; i < Count; i++) {
  for (int k = Count - 1; k > i; k--) {
  printf(" ");
  }
  for (int j = 0; j <= i; j++) {
  printf("*");
  }
  printf("\n");
  }
}
